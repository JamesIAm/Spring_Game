plugins {
    id "com.github.node-gradle.node" version "3.2.1"
}

task cleanup(type: Exec) {
    commandLine = ["bash", "-c", "lsof -ti :3000 | xargs kill"]
}

task start(type: Exec, dependsOn: cleanup) {
    commandLine = ["bash", "-c", "yarn start"]
//    finalizedBy(cleanup)
}

